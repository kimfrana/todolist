import { css } from '@emotion/core';

const input = css`
    width: 30px;
    
`;

const btn = css`
    font-size:40px;
    width:30px;
    background-color: red;
`;

export default {
    input,
    btn,
};
